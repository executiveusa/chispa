name: Validate Chispa
on:
  push:
    branches: [main]
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check JavaScript syntax
        run: |
          node --check app.js
          node --check migration.js
          node --check cloud-sync.js
          node --check files-ui.js
          node --check sw.js
          node --check api/sync.js
          node --check api/sync-health.js
          node --check api/files.js
          node --check api/files-health.js
      - name: Check required PWA and sync files
        run: |
          test -s index.html
          test -s styles.css
          test -s extras.css
          test -s cloud-sync.css
          test -s files-ui.css
          test -s manifest.webmanifest
          test -s icon.svg
          test -s supabase/migrations/001_chispa_project.sql
          test -s supabase/migrations/002_managed_supabase_snapshot.sql
          test -s supabase/migrations/003_harden_chispa_rls_rpc_boundary.sql
          test -s supabase/migrations/004_chispa_private_storage.sql
          test -s supabase/migrations/005_chispa_private_files_postgres.sql
          python -m json.tool manifest.webmanifest >/dev/null
      - name: Guard against committed privileged secrets
        run: |
          ! grep -R --exclude-dir=.git -E 'SUPABASE_SERVICE_ROLE_KEY[[:space:]]*=[[:space:]]*['"'\''][^$]' .
          ! grep -R --exclude-dir=.git -E 'postgres(ql)?://[^[:space:]]+:[^[:space:]]+@' .
